{
  "_comment": "Tachyon PTP side-B config template. Add slave-side PTP settings from a device config export.",
  "system": {
    "hostname": "{{hostname}}",
    "name": "{{hostname}}"
  },
  "wireless": {
    "radios": {
      "wlan0": {
        "vaps": [
          { "ssid": "{{ssid}}" }
        ]
      }
    }
  }
}
