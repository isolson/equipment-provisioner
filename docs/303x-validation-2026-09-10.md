# TNA-303X bench validation — 2026-09-10

The provisioner discovered the 303X on physical port 2 (isolation VLAN 1992).
The 303L remains on port 4. Both 303X firmware banks report 1.15.1 rev 55177,
with bank 1 active.

## Initial failure and template correction

Automatic provisioning had already run before the first diagnostic capture.
Login, firmware step 1, config upload, and config verification passed, but
secret application failed. This initial capture is post-attempt evidence, not
an untouched device baseline.

The 303X SM template omitted station profiles. Independent readback found zero
profiles, matching SNMP, and working standard root authentication. The handler
correctly refused to declare Wi-Fi secret application successful without profiles.

Added the four secret-free fleet station profiles used by the validated 303L
baseline to the 303X SM template. All other 303X template fields, including
radio and antenna settings, were preserved. The template was parsed again and
compared with the intended object before installation. Backups of both runtime
and bundled templates are retained privately. No handler code changed.

Template SHA-256:
`7b5558bac153a9722fd7b7852eba5934d95e5ae98e4d8677447d3620cfc2750d`

## Recovery acceptance

A provisioning retry on port 2 passed every configured step. Independent
readback confirmed:

- All 79 baseline fields match.
- Four station profiles are present and all keys match the host setting.
- SNMP matches the host setting.
- Standard root login and authenticated config access succeed.
- Factory and empty root logins are rejected as authentication failures.
- Non-password account fields are preserved.

The template uses legacy `network.ethernet`; comparison maps that section to
its documented API destination, top-level `ethernet`. A first raw-path comparison
reported seven missing legacy paths. Correcting the comparator's paths, without
changing device configuration, confirmed all 79 expectations.

## Evidence and remaining qualification

Private host evidence:
`/var/lib/provisioner/bench-evidence/tachyon/TNA-303X/live-2026-09-10/`

Raw backups, port responses, review summaries, template backups, and deployment
hash records stay private. Factory run 1 also passed every automatic step after a captured reset baseline
with default login accepted and zero station profiles. Independent readback
again confirmed all 79 baseline fields, four matching profile keys, matching
SNMP, preserved non-password account fields, standard root login/config access,
and rejection of factory and empty credentials. The factory root password was
therefore replaced by automatic provisioning, not inherited from the operator's
starting configuration.

Cold power-cycle persistence remains open. The operator subsequently confirmed
AP connection and management VLAN connectivity; those are operator-verified
passes, as recorded below. Both firmware
banks were already current; this run does not qualify an older-firmware upgrade.
The web service and kiosk watchdog were restored after factory baseline capture.

## Operator network acceptance

On 2026-09-10 the operator confirmed that the 303X passed AP connection and
management VLAN connectivity. Record these network checks as operator-verified
passes. Final power-cycle persistence remains open.
